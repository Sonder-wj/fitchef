# FitChef 项目解读报告

> 健身饮食知识库 RAG 问答系统 — 从架构到流程的完整解读

---

## 一、项目概述

**FitChef** 是一个面向健身饮食领域的 AI 问答平台。用户用自然语言提问（如"鸡胸肉怎么做好吃不柴"），系统从营养知识库中检索相关文档，结合大模型生成带来源引用的专业回答。

**核心能力：**
- 知识库包含食材、菜谱、膳食指南等文档
- 支持流式对话，回答逐字展示
- 自动拦截与饮食无关的问题
- 对话记忆：跨轮次理解上下文
- 检索质量可评测（55 题评测集）
- LangFuse 全链路可观测性追踪

---

## 二、技术栈

```
┌──────────────────────────────────────────────────────────┐
│  前端                  后端                       基础设施 │
│  Vue 3 (Composition)   FastAPI (异步)             Docker  │
│  Vite 8                 Python 3.13+              MySQL 8 │
│  纯 CSS (无框架)        uvicorn                   Milvus  │
│  fetch Streams API      SQLAlchemy 2.0 (异步)      etcd    │
│                         OpenAI SDK (DeepSeek)      MinIO   │
│                         SentenceTransformers     PostgreSQL│
│                         pymilvus                 LangFuse  │
│                         rank-bm25 + jieba        Ollama   │
│                         Loguru (日志)                     │
│                         Langfuse SDK (可观测)              │
└──────────────────────────────────────────────────────────┘
```

---

## 三、整体架构

```
┌──────────┐    SSE流式 (POST /chat/rag)     ┌──────────────────┐
│  Vue 3   │ ───────────────────────────────> │  FastAPI (8000)   │
│  前端     │ <─ query_rewrite, sources,      │  中间件: CORS     │
│  :5173   │    search_results, off_topic,    │       日志记录    │
│          │    text tokens...                │  鉴权: JWT Bearer │
└──────────┘                                 └───────┬──────────┘
                                                     │
                                          ┌──────────┴──────────┐
                                          │   RAG 检索链路       │
                                          │  查询改写 → 话题拦截  │
                                          │  → 混合检索 → 重排序  │
                                          │  → LLM 生成          │
                                          └──────────┬──────────┘
                                                     │
                              ┌──────────────────────┴──────┐
                              │  MySQL (用户/会话/消息)      │
                              │  Milvus (向量存储与检索)     │
                              │  Ollama (bge-m3 embedding)  │
                              │  DeepSeek API (LLM)         │
                              │  LangFuse (LLM 可观测追踪)   │
                              └─────────────────────────────┘
```

全部服务通过 Docker Compose 一键部署，容器间通过 Docker 网络通信。开发阶段 Vite dev server (:5173) 通过代理转发 `/auth`、`/chat` 到后端 (:8000)。

---

## 四、模块划分与职责

### 4.1 后端目录结构

```
app/
├── core/              # 基础设施层
│   ├── config.py        Pydantic 配置中心，从 .env 加载所有设置
│   ├── database.py      异步 SQLAlchemy 引擎 + 会话工厂
│   ├── security.py      JWT 令牌创建/验证，OAuth2 依赖注入
│   ├── middleware.py     HTTP 请求日志中间件
│   ├── logger.py        Loguru 日志配置（控制台 + 文件）
│   └── hashing.py       bcrypt 密码哈希与验证
│
├── models/            # ORM 模型（数据库表）
│   ├── user.py          users 表
│   ├── conversation.py  conversations 表 + DialogueType 枚举
│   └── message.py       messages 表
│
├── schemas/           # Pydantic 数据模型（请求/响应校验）
│   ├── user.py          UserCreate, UserLogin, UserResponse, Token
│   ├── chat.py          ChatRequest
│   ├── message.py       MessageResponse
│   └── conversation.py  ConversationResponse
│
├── routers/           # HTTP 路由层
│   ├── auth.py          /auth/register, /auth/login, /auth/me
│   └── chat.py          /chat/rag (SSE流式), 会话CRUD, 评测, 知识库管理
│
├── services/          # 业务逻辑层
│   ├── rag_chat_service.py         RAG 全链路编排（核心）
│   ├── query_rewriter_service.py   查询改写（LLM 输出 keywords + semantic）
│   ├── hybrid_search_service.py    混合检索（BM25 + 向量，RRF 融合）
│   ├── bm25_service.py            BM25 关键词检索
│   ├── embedding_service.py       Milvus 向量嵌入与检索
│   ├── reranker_service.py        Cross-Encoder 重排序
│   ├── fitchef_loader.py          知识库文档加载器
│   ├── conversation_service.py    会话/消息持久化 + 摘要生成
│   ├── user_service.py            用户注册/登录/查询
│   ├── observability.py           LangFuse LLM 可观测追踪
│   └── eval_service.py            检索质量评测
│
├── data/              # 知识库原始数据（CSV, JSON, Markdown）
└── indexes/           # Milvus 向量索引持久化目录
```

### 4.2 前端目录结构

```
frontend/
├── index.html              HTML 入口
├── vite.config.js          Vite 配置（代理到 :8000）
└── src/
    ├── main.js              Vue 应用创建
    ├── App.vue              根组件（登录/聊天条件渲染）
    ├── services/
    │   └── api.js           所有 API 调用 + SSE 流式处理
    └── components/
        ├── LoginForm.vue     登录/注册表单
        ├── ChatLayout.vue    主布局（侧边栏 + 聊天 + 统计）
        ├── Sidebar.vue       会话列表面板
        ├── ChatArea.vue      聊天消息 + 输入框 + 流式处理
        ├── MessageBubble.vue 单条消息气泡（支持 **加粗**）
        └── StatsBadge.vue    知识库统计弹窗
```

---

## 五、核心业务流程（按执行顺序）

用户发送 "鸡胸肉怎么做好吃又不柴"，完整的 13 步流程：

### 阶段一：入口处理

```
Step 0  POST /chat/rag 收到请求
        ├── JWT 鉴权：提取 Bearer Token → 解码 → 查用户
        ├── 会话管理：无 convId 则创建新会话，有则从 DB 加载
        ├── 加载历史消息（最近 10 条）+ 摘要
        └── 创建 StreamingResponse (text/event-stream)
```

### 阶段二：查询改写与话题拦截

```
Step 1  构建上下文查询
        输入:  原始问题 + 摘要 + 最近4条历史
        输出:  "对话背景: {summary} | 最近对话: ... | 当前问题: {query}"

Step 2  查询改写 (DeepSeek LLM)
        Prompt: 要求输出 JSON，含 keywords 和 semantic
        示例:   "鸡胸肉怎么做好吃又不柴"
              → keywords: "鸡胸肉 烹饪 嫩 不柴 做法"
              → semantic: "鸡胸肉怎么烹饪才能嫩而不柴"
        SSE事件: query_rewrite

Step 3  话题拦截（第一层）
        如果 keywords 为空 或 keywords == 原问题:
          → 输出 off_topic，结束（一步检索都不做）
```

### 阶段三：检索

```
Step 4  混合检索
        BM25 路:  keywords → jieba 分词 → BM25Okapi → top_k*3 结果
        向量路:  semantic → Ollama bge-m3 → Milvus 搜索 → top_k*3 结果
        融合:    RRF (Reciprocal Rank Fusion), k=60, min_score=0.01
        返回:    最多 12 个候选文档

Step 5  Cross-Encoder 重排序
        模型:    BAAI/bge-reranker-v2-m3
        方式:    对 (query, doc) 逐对打分 → 重新排序
        上限:    8 篇
        SSE事件: rerank

Step 6  精确短语匹配加权
        提取查询中的菜名/食材短语 → 匹配文档标题 → 分数翻倍
        效果:    "番茄炒蛋" 的文档分与 "番茄蛋花汤" 拉开 2 倍

Step 7  分数落差动态截断
        相邻文档分数骤降到 85% 以下 → 截断
        硬上限:   5 篇
```

### 阶段四：二次拦截

```
Step 8  话题拦截（第二层，兜底）
        最高分 < 0.005 → off_topic
        关键设计: 此检查在 search_results 和 sources 事件之前
```

### 阶段五：LLM 生成

```
Step 9  构建上下文
        文档格式化为 [1] content\n---\n[2] content...
        提取来源元数据（食材/食谱/膳食指南）
        SSE事件: sources, search_results

Step 10 LLM 流式生成 (DeepSeek)
        System Prompt 两种情况:
        ├── 正常分数: FITCHEF_SYSTEM_PROMPT（含检索文档）
        └── 低分(<0.003): LOW_SCORE_PROMPT（明确告知未找到）
        
        messages = [system_prompt + summary] + history[-4:] + [user_query]
        temperature=0.5, stream=True
        每个 token → SSE data: "token字符串"
```

### 阶段六：后处理

```
Step 11 消息持久化
        用户消息 + AI 回复 → MySQL messages 表

Step 12 摘要更新（后台异步，不阻塞响应）
        触发条件: 每3轮 / 首轮 / token估算 > 2000
        方式:     旧摘要 + 最近对话 → LLM → 新 2-3 句摘要
        存回:     conversations.summary

Step 13 返回 conversation_id
        SSE事件: conversation_id
```

---

## 六、数据流转图

```
┌─ 用户输入 ───────────────────────────────────────────────────────────┐
│                                                                       │
│  "鸡胸肉怎么做好吃又不柴"                                              │
│       │                                                               │
│       ├── 鉴权 → JWT 解码 → 查 MySQL users 表                          │
│       ├── 会话 → conversations 表（创建/复用）                         │
│       └── 历史 → messages 表（最近 10 条）                              │
│              │                                                        │
│              ▼                                                        │
│  ┌───────────────────── RAG 链路 ──────────────────────┐              │
│  │                                                     │              │
│  │  查询改写 ──→ 话题拦截 ──→ 混合检索 ──→ 重排序       │              │
│  │  (DeepSeek)    (关键词判)   (BM25+向量) (CrossEnc)  │              │
│  │      │                          │                    │             │
│  │      │                    ┌─────┴──────┐            │              │
│  │      │                    │ Milvus      │            │              │
│  │      │                    │ 向量数据库   │            │              │
│  │      │                    │ collection: │            │              │
│  │      │                    │ fitchef_    │            │              │
│  │      │                    │ knowledge   │            │              │
│  │      │                    └──────────────┘            │              │
│  │      │                    知识库文档                  │              │
│  │      │                    （食材/菜谱/                │              │
│  │      │                    膳食指南）                  │              │
│  │      │                                               │              │
│  │      ▼                                               │              │
│  │  动态截断 → 二次拦截 → LLM生成 → 保存 → 摘要更新      │              │
│  │                         (DeepSeek)  (MySQL)  (后台)    │              │
│  └─────────────────────────────────────────────────────┘              │
│              │                                                        │
│              ▼                                                        │
│  ┌───────────────────── SSE 事件流 ──────────────────────┐            │
│  │  query_rewrite → rerank → search_results → sources    │            │
│  │  → "做" → "凉" → "拌" → "鸡" → "丝" → ...token流     │            │
│  │  → conversation_id                                    │            │
│  └─────────────────────────────────────────────────────┘            │
│              │                                                        │
│              ▼                                                        │
│  ┌───────────────────┐                                               │
│  │  ChatArea.vue      │                                               │
│  │  逐token渲染       │                                               │
│  │  状态行+来源栏     │                                               │
│  └───────────────────┘                                               │
│                                                                       │
│  ┌───────────────────┐                                               │
│  │  LangFuse 追踪     │  全链路耗时 + 输入输出自动记录                 │
│  │  (可观测性)        │                                               │
│  └───────────────────┘                                               │
└───────────────────────────────────────────────────────────────────────┘
```

---

## 七、关键函数/类作用一览

| 文件 | 关键函数/类 | 作用 |
|------|------------|------|
| `config.py` | `Settings` | Pydantic 配置中心，从 .env 加载所有参数 |
| `database.py` | `get_db()` | FastAPI 依赖注入，提供请求级 AsyncSession |
| `security.py` | `create_access_token()` | JWT 生成，默认 24 小时过期 |
| `security.py` | `get_current_user()` | JWT 解码 + 查库，返回 User ORM |
| `hashing.py` | `hash_password()` / `verify_password()` | bcrypt 密码处理 |
| `rag_chat_service.py` | `RAGChatService.generate_stream()` | **核心编排器**，异步生成器，yield SSE 事件 |
| `query_rewriter_service.py` | `QueryRewriterService.rewrite()` | LLM 调用，输出 `{keywords, semantic}` |
| `hybrid_search_service.py` | `HybridSearchService.search()` | BM25 + 向量并行检索 + RRF 融合 |
| `hybrid_search_service.py` | `HybridSearchService._rrf_fusion()` | RRF 倒数排名融合算法 |
| `embedding_service.py` | `EmbeddingService.search()` | Ollama embedding + Milvus 向量相似度搜索 |
| `reranker_service.py` | `RerankerService.rerank()` | Cross-Encoder 重排序 |
| `conversation_service.py` | `ConversationService.save_message()` | 保存一轮对话（用户+AI）到 MySQL |
| `conversation_service.py` | `ConversationService.update_summary()` | LLM 压缩对话历史为摘要 |
| `observability.py` | `get_langfuse_trace()` | LangFuse trace 创建，追踪 RAG 全链路 |
| `eval_service.py` | `EvalService.run_eval()` | 三种策略对比评测，返回 Hit Rate + MRR |
| `ChatArea.vue` | `send()` | 发送消息，管理 messages 数组 |
| `ChatArea.vue` | `onToken()` / `onEvent()` / `onDone()` | SSE 回调，处理流式数据 |
| `api.js` | `sendMessage()` | fetch Streams API 实现 SSE 读取 |
| `MessageBubble.vue` | `renderMd()` | 简单 Markdown 渲染（**加粗**） |

---

## 八、入口与执行机制

### 8.1 后端入口

```bash
# 开发模式（直接用 Python）
python run.py
# 等价于: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

# 生产模式（Docker）
docker compose up -d
```

启动时序（`main.py` lifespan）：

```
1. Python 执行模块级代码
   ├── logger.py: 配置 Loguru（控制台+文件输出）
   ├── config.py: 创建 Settings 单例，解析 .env
   ├── database.py: 创建 AsyncEngine + SessionLocal
   └── security.py: 创建 OAuth2PasswordBearer 实例

2. FastAPI 实例化 app = FastAPI(lifespan=lifespan)
   ├── 注册 CORSMiddleware
   └── 注册 LoggingMiddleware

3. 注册路由
   ├── auth.router → /auth/*
   └── chat.router → /chat/*

4. ASGI 服务器就绪 → 进入 lifespan
   ├── create_all → 自动建表
   ├── EmbeddingService 初始化 → 连接 Milvus + 加载 Ollama 模型
   ├── FitChef 知识库加载 → BM25 索引 → 向量嵌入写入 Milvus
   ├── LangFuse 初始化（如已配置密钥）
   ├── rag_chat_service.is_ready = True
   ├── 后台预热查询改写 + 重排序模型
   └── yield → 开始接收请求
```

### 8.2 前端入口

```bash
cd frontend && npm run dev
# Vite dev server → http://localhost:5173
```

启动时序：
```
1. index.html 加载 → 挂载 #app
2. main.js: createApp(App).mount('#app')
3. App.vue: 检查 localStorage 中的 token
   ├── 无 token → 显示 LoginForm
   └── 有 token → 显示 ChatLayout
4. ChatLayout → Sidebar(会话列表) + ChatArea(聊天)
```

### 8.3 依赖注入链（以 POST /chat/rag 为例）

```
请求到达
  → CORSMiddleware（跨域）
  → LoggingMiddleware（日志）
  → FastAPI 路由匹配
  → Depends(get_current_user):
      1. OAuth2PasswordBearer 提取 Bearer token
      2. jwt.decode(token, SECRET_KEY)
      3. UserService.get_user_by_email(email)
  → Depends(get_db):
      1. AsyncSessionLocal() 创建会话
      2. yield session → 路由函数使用
      3. 路由返回后: commit / rollback / close
```

---

## 九、依赖关系

### 9.1 Python 核心依赖

| 包名 | 用途 |
|------|------|
| fastapi | Web 框架，路由、依赖注入、API 文档 |
| uvicorn | ASGI 服务器 |
| sqlalchemy[asyncio] + aiomysql | 异步 ORM + MySQL 驱动 |
| openai | DeepSeek API 调用（兼容 OpenAI SDK） |
| pydantic + pydantic-settings | 数据校验 + 配置管理 |
| sentence_transformers | Cross-Encoder 重排序模型 |
| pymilvus | Milvus 向量数据库客户端 |
| rank-bm25 + jieba | BM25 关键词检索 + 中文分词 |
| loguru | 结构化日志 |
| python-jose + passlib + bcrypt | JWT + 密码哈希 |
| PyPDF2 + python-docx | 知识库文档解析 |
| langfuse | LLM 可观测性追踪 |

### 9.2 前端依赖

| 包名 | 用途 |
|------|------|
| vue 3.5 | 前端框架 |
| vite 8 | 构建工具 |
| @vitejs/plugin-vue | Vite Vue 插件 |

### 9.3 外部服务依赖

| 服务 | 用途 | 容器名 |
|------|------|--------|
| MySQL 8.0 | 用户/会话/消息持久化 | fitchef-mysql |
| Milvus 2.5 | 向量存储与相似度检索 | fitchef-milvus |
| etcd 3.5 | Milvus 元数据协调 | fitchef-etcd |
| MinIO | Milvus 对象存储 | fitchef-minio |
| PostgreSQL 16 | LangFuse 数据存储 | fitchef-langfuse-db |
| LangFuse | LLM 可观测性平台 | fitchef-langfuse |
| DeepSeek API | LLM 生成 + 查询改写 | 外部 API |
| Ollama | 文本 Embedding (bge-m3) | 宿主机 :11434 |

---

## 十、部署与运行方式

### Docker 全栈一键部署

```bash
# 1. 克隆项目
git clone <repo-url> && cd my_project1

# 2. 配置环境变量（参考下方 .env 说明）
cp .env.example .env
# 编辑 .env，填入 DEEPSEEK_API_KEY 等必要配置

# 3. 一键启动全部 8 个服务
docker compose up -d

# 4. 访问 http://localhost:5173
```

**服务列表：**

| 服务 | 端口 | 说明 |
|------|------|------|
| MySQL 8.0 | 3306 | 业务数据库 |
| etcd | 2379 | Milvus 元数据 |
| MinIO | 9000 | Milvus 对象存储 |
| Milvus 2.5.0 | 19530 | 向量数据库 |
| PostgreSQL 16 | 5432 | LangFuse 数据 |
| LangFuse | 3000 | LLM 可观测平台 |
| FastAPI 后端 | 8000 | API 服务 |
| Vue 3 前端 | 5173 | Web 界面 |

**常用命令：**

```bash
docker compose down               # 停止全部
docker compose up -d              # 启动全部
docker compose up -d --build backend   # 重建后端
docker compose up -d --build frontend  # 重建前端
docker compose logs -f backend    # 查看后端日志
```

### 关键配置项 (.env)

```ini
# DeepSeek
DEEPSEEK_API_KEY=sk-xxx
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1/
DEEPSEEK_MODEL=deepseek-v4-flash

# Ollama Embedding（容器内必须用 host.docker.internal）
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_EMBEDDING_MODEL=bge-m3

# Milvus
MILVUS_HOST=milvus
MILVUS_PORT=19530
MILVUS_COLLECTION=fitchef_knowledge

# MySQL
DB_HOST=mysql
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_NAME=my_project1

# JWT
SECRET_KEY=your-secret-key-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# RAG
RAG_TOP_K=5
RAG_USE_RERANK=True
RAG_USE_QUERY_REWRITE=True

# LangFuse（可选，不配则自动禁用）
LANGFUSE_PUBLIC_KEY=pk-xxx
LANGFUSE_SECRET_KEY=sk-xxx
LANGFUSE_HOST=http://langfuse:3000
```

> **注意：** 容器内服务间通信使用 Docker 服务名（如 `mysql`、`milvus`），不能写 `localhost`。Ollama 运行在宿主机上，通过 `host.docker.internal` 访问。

### 数据库表关系

```
users (1) ───< conversations (N) ───< messages (N)

users:           id, username, email, password_hash, status
conversations:   id, user_id(FK), title, summary, dialogue_type(RAG/NORMAL)
messages:        id, conversation_id(FK), sender(user/assistant), content, created_at
```

两个外键均设置 `ON DELETE CASCADE`，删用户→删会话→删消息全链路级联。

---

## 十一、系统亮点

1. **双路查询改写**：同一个 LLM 调用输出 keywords（给 BM25）+ semantic（给向量），两路检索各取所需，零额外开销。

2. **双层话题拦截**：查询改写后关键词为空→直接拒绝（不检索）；检索后分数<0.005→兜底拒绝（不发来源给前端）。既省算力，用户体验也干净。

3. **RRF 融合 + Cross-Encoder 重排序**：排名融合解决 BM25 和向量分数不可比的问题，重排序大幅提升 Top-1 准确率。

4. **分数落差动态截断**：不写死 top_k，用相邻文档的分数比值自适应截断，避免低分噪音混入 LLM 上下文。

5. **摘要缓冲记忆**：LLM 主动压缩历史为 2-3 句摘要，token 用量恒定不随轮次增长。异步后台更新不阻塞响应。

6. **每层都有降级**：查询改写失败→回退原查询，Milvus 不可用→降为纯 BM25，重排序加载失败→跳过，LLM 异常→返回错误提示。系统不会因单点故障整体不可用。

7. **LangFuse 全链路可观测**：每次 RAG 调用的查询改写、检索、重排序、LLM 生成各阶段耗时和输入输出自动追踪，调试和优化有数据支撑。

8. **检索质量可量化**：55 题评测集，Hit Rate + MRR 双指标，三种策略自动对比。

9. **Docker 一键部署**：8 个服务一条命令启动，换机器零配置，开发环境与生产环境一致。

---

## 附录：SSE 事件类型速查

| 事件类型 | 含义 | 前端处理 |
|----------|------|----------|
| `query_rewrite` | 查询改写结果 | 显示关键词在状态行 |
| `rerank` | 重排序对比 | 显示"重排序完成" |
| `search_results` | 检索到的文档 | 展示在可折叠面板 |
| `sources` | 参考来源列表 | 显示在来源栏 |
| `off_topic` | 话题拦截 | 替换机器人消息为拒绝文案 |
| `conversation_id` | 会话 ID | 更新当前会话 ID |
| `error` | 错误 | 显示错误信息 |
| (字符串 token) | LLM 生成文本 | 逐字追加到机器人消息 |
