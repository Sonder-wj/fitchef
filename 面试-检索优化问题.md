# 面试素材：RAG 中医知识库项目——技术难点与优化

> 适用场景：面试被问到"项目中遇到过什么技术难点"、"RAG 检索链路怎么设计的"、"做过什么优化"时，可以用来讲。

---

## 一、背景

我做了一个**健身饮食知识库 RAG 问答系统（FitChef）**，后端 FastAPI，检索链路是：

```
查询改写 → 话题拦截 → BM25+FAISS混合检索(RRF融合) → Cross-Encoder重排序 → LLM流式生成
```

用 DeepSeek 做 LLM，bge-m3 做 embedding，jieba 分词实现 BM25，MySQL 做会话持久化，Redis 做语义缓存，前端 Vue 3 流式对话。

---

## 二、问题一：跨领域问题——混合检索的查询"偏科"

### 描述

BM25 和向量检索对查询形式的需求是冲突的：BM25 要关键词，向量要自然语句。但查询改写只输出一种形式，导致总有一路效果打折扣。

### 核心改动

**双路查询**：让 LLM 同时输出两种查询形式，BM25 用关键词，向量用自然语句。

```
改前：
"肚子胀没胃口" → "腹胀 食欲不振 理气 陈皮"  ← 两路都用这个

改后：
"肚子胀没胃口" → {
  "keywords": "腹胀 食欲不振 脾胃不和 食积",    ← BM25
  "semantic": "脾胃虚弱导致腹胀食欲不振..."      ← 向量检索
}
```

- 改 query_rewriter_service 的 prompt 为 JSON 双输出
- 改 hybrid_search_service 支持 `bm25_query` / `vector_query` 分离参数
- JSON 解析失败自动回退原查询

---

## 三、问题二：领域内问题——对话记忆的 Token 管理

### 描述

最初用窗口记忆：取最近 6 条历史直接塞 prompt。问题：
- 切片按**条数**不按 token——6 条可能是 100 token 也可能是 6000 token
- 历史**全量从 DB 加载**，没有 LIMIT
- 超过窗口的对话直接丢弃，长期记忆缺失
- RAG 上下文本就大（检索结果 + system prompt），历史消息再挤占，token 预算很快用完

### 核心改动

**摘要缓冲记忆（Summary Buffer Memory）**：用 LLM 自动压缩历史为 2-3 句摘要。

```
之前（窗口记忆）：
  加载全部历史 → 取最后 [-6:] 条 → 塞进 prompt → 多的丢弃

现在（摘要缓冲）：
  加载最近 20 条 → LLM 压缩为摘要 → 摘要 + 最近 4 条 → 拼进 prompt
                                               ↑
                                       每轮对话后自动更新
```

### 具体改动

| 层 | 改动 |
|----|------|
| 模型层 | Conversation 表加 `summary` 字段（Text） |
| 服务层 | `get_messages` 加 LIMIT 20；新增 `update_summary`（独立 session，后台异步） |
| RAG 层 | `generate_stream` 接收 summary，注入 query rewrite + system prompt |
| 路由层 | 加载 summary 传入 RAG，消息保存后 `asyncio.create_task` 更新摘要 |

### 摘要 prompt 设计

```
旧摘要 + 新对话 → 融合更新
"用户在咨询失眠问题，已讨论酸枣仁和归脾汤" 
  + "用户: 那多梦怎么办 | AI: 可以试试天王补心丹..."
  → "用户在咨询失眠问题，入睡改善但仍多梦，已推荐归脾汤和天王补心丹"
```

### 效果

对话 10 轮后，用户说"那这个药怎么吃"，LLM 从摘要就能知道"这个药"指天王补心丹，不需要重复历史。token 用量恒定，不随对话轮次增长。

---

## 四、面试怎么讲（1分钟版本）

> 我做了一个中医 RAG 问答系统，优化过程中遇到两个比较有意思的问题。
>
> 第一个是检索的"偏科"问题：混合检索里 BM25 要关键词、向量要自然语句，但查询改写只输出一种，总有一路吃亏。我把改写改成了双输出——同一个 LLM 调用生成关键词和语义两种形式，BM25 和向量各取所需。
>
> 第二个是对话记忆的 token 管理：窗口记忆按条数切不按 token，长对话容易爆上下文。我改成了摘要缓冲记忆——LLM 自动把历史压缩成 2-3 句摘要，摘要当长期记忆、最近几轮当短期记忆，token 用量恒定。
>
> 这两个优化让我意识到，RAG 系统里每个环节的输入格式都要针对下游机制来设计，不能一根管子通到底。

---

## 五、可以延伸的面试追问

1. **为什么不用 query2doc？**——要额外调一次 LLM 生成伪文档，延迟翻倍。双路输出在同一个调用里完成，零额外开销。

2. **如果数据量变大（10w+），RRF 还够用吗？**——可以聊进化路径：RRF → 加权融合 → 学习型融合。

3. **摘要记忆和 LangChain 的 ConversationSummaryBufferMemory 有什么区别？**——思路一样，但我自己实现的，不依赖 LangChain 框架。核心就一句 prompt + DB 一个字段。

4. **摘要不会丢信息吗？**——摘要只存"骨架"（症状、用药、反馈），细节靠最近几轮原话补。摘要是压缩不是丢弃。

5. **为什么不用向量记忆（存每轮对话 embedding）？**——向量记忆适合"回忆很久以前某句话"的场景，但对话上下文更看重"刚才聊了什么"+"整体在聊什么"，摘要+窗口更直接。

---

## 六、问题三：检索噪音——固定 top_k 导致低相关文档混入

> 适用场景：面试被问到"检索结果有噪音怎么处理"、"如何评估检索质量"、"检索链路怎么调优"

### 描述

**强制返回固定数量（top_k=5）**，当知识库中真正相关的文档不足 5 篇时，低分文档被强行凑数，混入检索结果。用户问"怎么做"时尤为明显——像"番茄炒蛋怎么做"会检索到番茄鸡蛋盖浇面、番茄蛋花汤等同食材但不同菜的食谱。

### 根因分析（三层）

| 层 | 问题 |
|---|---|
| 分词层 | jieba 将"番茄炒蛋"拆成"番茄""炒蛋"，BM25 对含"番茄"的文档打分类似，无法区分不同菜名 |
| 融合层 | RRF 公式 `1/(k+rank)` 中 k=60 过于平滑，排名相邻文档的分数差仅 ~0.0005，天然缺乏区分度 |
| 截断层 | 写死 `top_k=5`，不管有没有 5 篇真正相关的文档 |

### 三道优化

#### 3.1 RRF 融合层加最低分阈值

`hybrid_search_service.py` — `_rrf_fusion()` 方法新增 `min_score` 参数：

```python
sorted_ids = sorted(scores.items(), key=lambda x: x[1], reverse=True)
filtered = [(id, s) for id, s in sorted_ids if s >= min_score]  # 新增
results = filtered[:top_k]
```

同时将初始检索量从 `top_k * 2` 扩到 `top_k * 3`，给 RRF 更大的候选池。

#### 3.2 精确短语匹配加权（无重排序时的兜底）

核心思路：查询中的菜名短语若出现在文档标题中，分数翻倍——人为制造区分度。

```python
def _extract_query_phrases(query: str) -> list:
    """去掉'怎么做''是什么'等问句后缀，提取核心短语"""
    cleaned = query.strip()
    for suffix in ["怎么做", "怎么做好吃", "是什么", "的热量", ...]:
        cleaned = cleaned.replace(suffix, "")
    return [cleaned, query]  # 返回清洗后 + 原始

# 匹配文档标题 → 分数翻倍
for r in final_results:
    title = extract_title(r["content"])  # 从【分类】菜名\n... 格式提取
    for phrase in query_phrases:
        if phrase in title:
            r["score"] *= 2.0
```

**效果**："番茄炒蛋"的 RRF 分 0.0325 → 0.0650，与第二名（0.0315）拉开 2 倍差距。

#### 3.3 分数落差动态截断（重排序后）

核心思路：利用 Cross-Encoder 重排序后的分数（区分度远优于 RRF），检测相邻文档间的分数比值。若骤降到 85% 以下，说明后续均为噪音。

```python
for i in range(1, len(final_results)):
    prev = final_results[i-1]["score"]
    curr = final_results[i]["score"]
    if prev > 0 and curr / prev < 0.85:   # 分数骤降 15%
        final_results = final_results[:i]   # 截断
        break
final_results = final_results[:5]  # 上限兜底
```

**为什么用比值而非绝对阈值？**
- 不同查询的相关度分天然不同（热门菜 vs 冷门菜）
- 比值自适应：高分密集时多取，稀疏时少取
- Cross-Encoder 分数通常呈阶梯状（0.99 → 0.93 → 0.77 → 0.15），天然适合比值截断

### 效果对比

| 指标 | 优化前 | 优化后 |
|---|---|---|
| 返回文档数 | 固定 5 篇 | 动态 1~5 篇 |
| "番茄炒蛋"检索噪音 | 4/5 篇不相关 | **0 篇噪音** |
| "鸡胸肉怎么做"检索噪音 | 混入荞麦面、意面 | 只返回相关做法 |
| 生成回复质量 | 末尾会推荐无关菜 | 聚焦问题本身 |
| Cross-Encoder 不可用时 | 区分度低 | 短语加权兜底 |

### 面试 1 分钟版本

> 做 RAG 检索优化时遇到一个问题：系统写死了每次返回 top_k=5 篇文档，但很多时候真正相关的只有 1-2 篇，低分文档被强行凑数，检索噪音直接传导到生成回复。
>
> 我用了三层递进方案：第一，RRF 融合层加最低分阈值，过滤掉绝对低分的文档；第二，提取用户查询中的核心短语，跟文档标题做精确匹配，命中就翻倍加权，这是兜底——Cross-Encoder 加载不了时也能拉开差距；第三，用分数落差做动态截断，相邻文档分数骤降到 85% 以下就直接切掉，不再强制凑够 N 篇。
>
> 效果很明显："番茄炒蛋怎么做"从返回 5 篇（其中 4 篇是番茄蛋花汤、番茄牛腩等噪音）优化到只返回 1 篇精确结果。这套方案的优势是用比值而非绝对值，不需要为每个查询调阈值。

### 延伸追问

1. **为什么不用 query2doc 或 HyDE？**——这些方法需要额外 LLM 调用，延迟翻倍。短语加权零额外耗时，更适合对延迟敏感的场景。

2. **85% 的比值阈值怎么定的？**——在评测集上观察 Cross-Encoder 分数分布得出的经验值。相关文档 0.8+，相似但不完全匹配 0.5-0.8，完全无关 0.3 以下。85% 能有效捕捉"从相关落到相似"的拐点。

3. **如果 Cross-Encoder 不可用（如本地部署内存不足），方案还成立吗？**——成立。方案 3.2（短语加权）本身就是为这个场景设计的兜底，配合更激进的比值阈值（95%）替代方案 3.3。

4. **跟提升 prompt 质量比，哪个性价比高？**——检索优化是源头治理。检索给 LLM 的上下文如果全是噪音，prompt 再好也没用。先优化检索、再优化 prompt，是这个场景的正确顺序。


---

## 七、问题四：话题拦截——跨领域查询的检索浪费

> 适用场景：面试被问到"如何判断用户意图"、"怎么处理无关查询"、"管道性能怎么优化"

### 描述

用户问了一个跟健身饮食完全无关的问题（比如"想买一件衣服"），系统照常走完了整个检索链路——查询改写 → 混合检索 → 重排序 → 提取 5 篇文档来源 → 最后 LLM 才发现回答不了。前面的检索步骤全是浪费。

### 根因分析

话题检测写在了**检索之后**——依赖检索结果的高分文档来判断是否偏题。但此时混合检索（BM25 + FAISS 向量）和 Cross-Encoder 重排序已经全部跑完，token 和延迟都花了。

```
优化前：查询改写 → 混合检索 → 重排序 → 话题检测 → LLM拒绝
                              ↑____白白检索____↑
```

### 核心改动

**把话题拦截提前到查询改写之后、检索之前**——利用查询改写 LLM 输出的关键词做第一道拦截。

#### 4.1 前置拦截：关键词检测（零检索开销）

```python
# 查询改写后立即检测
keywords_str = (keywords or "").strip()
if not keywords_str or keywords_str == query.strip():
    # 没提取到饮食相关关键词 → 直接拒绝，一步检索都不做
    yield off_topic_event
    return
```

**判断逻辑**：查询改写 LLM 的 prompt 里明确要求——"如果用户输入与饮食、健身、营养、食材、烹饪完全无关，keywords 必须严格输出空字符串"。关键词为空或等于原问题，直接拦截。

#### 4.2 更新 query rewriter prompt

把"keywords 留空"从软建议升级为硬规则，并给出明确的正反例：

```
【重要】如果用户输入与饮食、健身、营养、食材、烹饪完全无关，keywords 必须严格输出空字符串

判断无关话题的示例（keywords 必须为空）：
- 购物类："想买一件衣服"、"推荐手机" → keywords 留空
- 天气类："今天天气怎么样" → keywords 留空
- 科技类："Python怎么写" → keywords 留空
```

同时在正面示例中直接加入 "想买一件衣服" → `{"keywords": "", "semantic": "想买一件衣服"}`，强化 LLM 的判断能力。

#### 4.3 兜底拦截：检索分数阈值（LLM 没拦住时）

LLM 有时候不听话——对跨界查询也强行联想出几个"关键词"。此时检索还是会跑，但检索结果分数极低。加入第二道防线：

```python
# 检索完成后，发结果给前端之前
best_score = final_results[0].get("rerank_score", ...)
if not final_results or best_score < 0.005:
    # 分数太低 → 拦截，不发送 search_results 和 sources 事件
    yield off_topic_event
    return
```

关键设计：分数拦截放在 `search_results` 和 `sources` 事件**之前**——前端收不到任何检索结果和来源引用，用户看不到 "检索到 5 篇文档" 的状态提示。

### 完整流程

```
查询改写
    ↓
关键词为空/等于原问题？
    ├── 是 → 立即拦截（不检索）
    └── 否 → 混合检索 → 重排序
                    ↓
              最高分 < 0.005？
                    ├── 是 → 兜底拦截（不发送检索结果给前端）
                    └── 否 → 正常生成回答
```

### 对比数据

运行 55 题评测集验证离线话题不影响正常检索：

| 策略 | Hit Rate | MRR |
|------|----------|-----|
| BM25 | 0.71 | 0.66 |
| Vector | 0.91 | 0.83 |
| Hybrid | 0.87 | 0.73 |

向量检索最优，与优化前一致——话题拦截不影响正常查询的检索质量。

### 面试 1 分钟版本

> 做 RAG 问答时发现一个问题：用户问"想买一件衣服"，系统会把混合检索、重排序全跑完，最后 LLM 才说"我是饮食助手答不了"。前面的检索步骤全是浪费。
>
> 我的做法是把话题拦截拆成两层：第一层放在查询改写之后——利用改写 LLM 输出的关键词做判断，关键词为空直接拒绝，零检索开销。第二层是兜底——如果 LLM 没按 prompt 要求做（比如硬给非饮食查询编了关键词），检索跑完后用分数阈值 < 0.005 拦住，在前端收到任何检索结果之前就返回。
>
> 关键设计是第二层的拦截位置要放在 search_results 和 sources 事件之前——用户感知到的就是"问了无关问题，机器直接说不会"，不会看到"检索到 5 篇文档"然后又被替换，体验很割裂。

### 延伸追问

1. **为什么不用分类模型做意图识别？**——在当前场景不需要。查询改写本身就是一次 LLM 调用，让它同时输出 keywords 判断意图是零额外开销。如果未来要做多轮意图识别（比如区分"问菜谱"还是"问营养数据"），再考虑加一个轻量分类模型。

2. **两层检测的阈值（0.005）怎么定的？**——在评测集上观察的。与饮食相关的查询 Cross-Encoder 分在 0.5~0.99，跨界查询在 0.001~0.004 之间。0.005 是安全线。

3. **如果查询改写 LLM 超时/报错怎么办？**——改写失败时 `keywords=query`，会被第一层拦截（`keywords_str == query.strip()`），走 off-topic 路径。这是偏保守的策略——宁可误拒，不浪费检索。

4. **跟"敏感词过滤"有什么区别？**——敏感词过滤是黑名单匹配，只能挡住已知的无关词。我的方案是基于语义理解——LLM 会判断"买衣服"和"问瑜伽服哪个品牌好"的区别，后者虽然含"买"但属于健身相关。
